<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Error - PHP Error Insight</title>
    <style>
        :root {
            --primary-color: #dc3545;
            --secondary-color: #6c757d;
            --success-color: #28a745;
            --info-color: #17a2b8;
            --warning-color: #ffc107;
            --light-color: #f8f9fa;
            --dark-color: #343a40;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            line-height: 1.6;
            color: var(--dark-color);
            background-color: #fff;
            margin: 0;
            padding: 2rem;
        }

        .error-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .error-header {
            background: linear-gradient(135deg, var(--primary-color), #c82333);
            color: white;
            padding: 2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .error-header h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .error-header .exception-class {
            font-size: 1.2rem;
            opacity: 0.9;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            background: rgba(255, 255, 255, 0.1);
            padding: 0.5rem 1rem;
            border-radius: 4px;
            display: inline-block;
            margin-top: 1rem;
        }

        .error-details {
            background: var(--light-color);
            padding: 2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            border-left: 4px solid var(--primary-color);
        }

        .error-details h2 {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .error-details h3 {
            color: var(--secondary-color);
            margin: 1.5rem 0 0.5rem 0;
            font-size: 1.2rem;
        }

        .error-message {
            background: white;
            padding: 1rem;
            border-radius: 4px;
            border: 1px solid #dee2e6;
            font-size: 1.1rem;
            margin-bottom: 1rem;
            word-break: break-word;
        }

        .error-location {
            background: white;
            padding: 1rem;
            border-radius: 4px;
            border: 1px solid #dee2e6;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 0.9rem;
        }

        .error-location strong {
            color: var(--primary-color);
        }

        .stack-trace {
            background: var(--dark-color);
            color: #fff;
            padding: 2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            overflow-x: auto;
        }

        .stack-trace h3 {
            color: #fff;
            margin-bottom: 1rem;
            font-size: 1.2rem;
        }

        .stack-trace pre {
            margin: 0;
            white-space: pre-wrap;
            word-break: break-all;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 0.85rem;
            line-height: 1.4;
        }

        .ai-section {
            background: linear-gradient(135deg, var(--info-color), #138496);
            color: white;
            padding: 2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
        }

        .ai-section h3 {
            margin-bottom: 1rem;
            font-size: 1.3rem;
        }

        .ai-section .ai-content {
            background: rgba(255, 255, 255, 0.1);
            padding: 1.5rem;
            border-radius: 4px;
            margin-top: 1rem;
        }

        .footer {
            text-align: center;
            padding: 2rem 0;
            color: var(--secondary-color);
            border-top: 1px solid #dee2e6;
            margin-top: 2rem;
        }

        .footer a {
            color: var(--info-color);
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }
            
            .error-header h1 {
                font-size: 2rem;
            }
            
            .error-details,
            .stack-trace,
            .ai-section {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-header">
            <h1>🔍 Application Error</h1>
            <div class="exception-class">{{ exception_class }}</div>
        </div>

        <div class="error-details">
            <h2>💬 Error Message</h2>
            <div class="error-message">{{ message }}</div>

            <h3>📍 Location</h3>
            <div class="error-location">
                <strong>File:</strong> {{ file }}<br>
                <strong>Line:</strong> {{ line }}
            </div>
        </div>

        {% if ai_explanation is defined and ai_explanation %}
            <div class="ai-section">
                <h3>🤖 AI-Powered Analysis</h3>
                <div class="ai-content">
                    {{ ai_explanation | nl2br }}
                </div>
            </div>
        {% endif %}

        {% if ai_suggestions is defined and ai_suggestions %}
            <div class="ai-section">
                <h3>💡 AI Suggestions</h3>
                <div class="ai-content">
                    {{ ai_suggestions | nl2br }}
                </div>
            </div>
        {% endif %}

        <div class="stack-trace">
            <h3>📚 Stack Trace</h3>
            <pre>{{ trace }}</pre>
        </div>

        <div class="footer">
            <p>Powered by <a href="https://github.com/raffaelecarelle/php-error-insight" target="_blank">PHP Error Insight</a> | 
            <a href="https://symfony.com" target="_blank">Symfony Bundle</a></p>
        </div>
    </div>
</body>
</html>